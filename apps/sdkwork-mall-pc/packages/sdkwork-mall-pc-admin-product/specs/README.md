# Component Specs

`component.spec.json` is the machine-readable component contract for `sdkwork-mall-pc-admin-product`.

The root SDKWork standards remain authoritative through relative paths into `../../../../../../sdkwork-specs/`.
